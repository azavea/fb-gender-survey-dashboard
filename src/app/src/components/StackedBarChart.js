import React from 'react';
import { Box, Text } from '@chakra-ui/react';

const StackedBarChart = ({ question, responses }) => {
    return (
        <Box>
            <Text>Stacked Bar Chart</Text>
        </Box>
    );
};

export default StackedBarChart;
